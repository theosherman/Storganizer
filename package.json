{
  "name": "storganizer",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter @storganizer/web test:e2e"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
